<!-- =================================================
    Copyright 2025 NeuMove Laboratory, Northeastern University
    Model   :: Terrain Pointer (myoassist-terrains Option B layout)
    Author  :: Calder Robbins (robbins.cal@northeastern.edu)
    License :: Apache 2.0
====================================================== -->
<!--
  Active terrain pointer. Model XMLs (26muscle_3D/myoLeg26_*.xml) include
  this file at root level. In turn it chains:

    1. ../terrain_style.xml    : shared style (skybox, materials, haze, lights)
    2. ../terrain/active.xml   : currently-active terrain content

  Path note: MuJoCo resolves nested includes relative to the TOP-LEVEL model
  file's directory (model/26muscle_3D/), not relative to this file. The
  "../" prefix climbs out of the model dir back to model/. If model files
  live at a different depth, adjust the prefix.

  To switch active terrain, change the second include line below, or run:
      python -m myoassist_terrains set-active NAME
-->

<mujocoinclude>
    <include file="../terrain_style.xml"/>
    <include file="../terrain/myoassist_base_tiled3x3.xml"/>
</mujocoinclude>
